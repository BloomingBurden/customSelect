# customSelect — Чистый JavaScript Custom Select (v1)

Простой, лёгкий и независимый кастомный `<select>` на Vanilla JS.  
Работает без зависимостей, настраивается через дата-атрибуты, легко интегрируется в любой проект.

---

## 🎯 Что это

customSelect — это замена стандартному `<select>` элементу с кастомным UI и поведением:

- styles-agnostic — вы сами пишете стили
- работает на чистом JavaScript
- поддерживает клавиатуру
- адаптивный
- минимальный и понятный код

---

## 🚀 Возможности

✅ Поддержка нескольких селектов  
✅ Переключение через клики  
✅ Стрелка для открытия/закрытия  
✅ Обработка выбранного значения  
✅ Чистая архитектура  
✅ Отсутствие зависимостей  
✅ Подходит под любые UI-шаблоны

---

## 📦 Установка

Просто подключи скрипт:

```html
<script src="customSelect.v1.js"></script>
```

Инициализация автоматически применяется ко всем элементам с классом `.custom-select`.

---

## 🧱 HTML структура

```html
<div class="custom-select">
    <div class="custom-select__input">
        <span class="custom-select__current">Выберите</span>
        <span class="custom-select__arrow"></span>
    </div>
    <div class="custom-select__dropdown">
        <div class="custom-select__option" data-value="1">Опция 1</div>
        <div class="custom-select__option" data-value="2">Опция 2</div>
        <div class="custom-select__option" data-value="3">Опция 3</div>
    </div>
</div>
```

---

## ⚙️ Как это работает

1. `customSelect.v1.js` находит все элементы с `.custom-select`
2. Перебирает каждый и навешивает события
3. Открывает/закрывает дропдаун
4. Обрабатывает выбор опций
5. Обновляет отображаемое значение

---

## 🎛 Data-атрибуты

| Атрибут | Назначение |
|---------|------------|
| `data-value` | Значение опции |
| `class="custom-select__current"` | Отображает выбранное значение |
| `class="custom-select__arrow"` | Стрелка (анимация через CSS) |

---

## 💡 API взаимодействие

Если нужно получить выбранное значение в коде:

```js
const select = document.querySelector('.custom-select');
const value = select.dataset.selectedValue;
console.log(value);
```

---

## 🧠 Обработка выбора

При выборе опции:

✔ Изменяется текст в `.custom-select__current`  
✔ Добавляется активный класс  
✔ Закрывается дропдаун  
✔ Значение записывается в `dataset`

---

## 📌 Пример использования

```html
<div class="custom-select">
    <div class="custom-select__input">
        <span class="custom-select__current">Пункт</span>
        <span class="custom-select__arrow"></span>
    </div>
    <div class="custom-select__dropdown">
        <div class="custom-select__option" data-value="apple">Apple</div>
        <div class="custom-select__option" data-value="banana">Banana</div>
        <div class="custom-select__option" data-value="cherry">Cherry</div>
    </div>
</div>

<script src="customSelect.v1.js"></script>
```

---

## 🧪 Поведение

✔ Открытие/закрытие по клику  
✔ Закрытие при клике вне компонента  
✔ Выбор опции  
✔ Поддержка кастомных стилей через CSS  
✔ Можно использовать в формах  

---

## 🧩 Стили

Скрипт **не содержит** встроенных стилей — это даёт свободу адаптировать визуал под любой дизайн.

Пример базовых стилей:

```css
.custom-select {
  position: relative;
  cursor: pointer;
}

.custom-select__dropdown {
  display: none;
  position: absolute;
  top: 100%;
  left: 0;
  width: 100%;
  z-index: 10;
}

.custom-select.open .custom-select__dropdown {
  display: block;
}

.custom-select__option:hover {
  background: rgba(0,0,0,0.1);
}
```

---

## 🧠 Особенности реализации

✔ Использует делегирование событий  
✔ Универсальный DOM-код  
✔ Без jQuery  
✔ Нативные методы `classList`, `querySelector`, `dataset`

---

## 🛠 Требования

Работает во всех современных браузерах с поддержкой ESModules и `classList`.

---

## 📄 Лицензия

MIT © BloomingBurden

